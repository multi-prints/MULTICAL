// Sidebar is implemented in app.rs (needs Page enum + navigation state).
// This module is kept so `components::sidebar` remains a valid path.
// Prefer the in-app Sidebar for layout work; do not reintroduce a second nav tree here.
